import { createVuetify } from 'vuetify';
import 'vuetify/styles';  // Ensure this is included for Vuetify styling

const vuetify = createVuetify({
  // Optionally you can configure your Vuetify options here
});

export default vuetify;
